


// grab the mute button to use below

var mute = document.querySelector('.mute');



// grab audio track via XHR for convolver node

soundUniform = {
data: {type: "iv1", value: new Uint8Array(256)}
};

console.log(soundUniform.data.value);

